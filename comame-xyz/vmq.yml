# Deployment

apiVersion: apps/v1
kind: Deployment
metadata:
  name: vmq
  namespace: comame-xyz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vmq
  template:
    metadata:
      labels:
        app: vmq
    spec:
      containers:
      - image: registry.comame.dev/vmq:latest
        name: vmq
        ports:
        - containerPort: 80
          protocol: TCP
      imagePullSecrets:
        - name: regcred

---

# Service

apiVersion: v1
kind: Service
metadata:
  name: vmq
  namespace: comame-xyz
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: vmq
